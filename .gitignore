node_modules/
dist/
*.log

# Local Claude Code settings
.claude/
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local ChromaDB store (never commit the vector store)
store/db/
.handoff/

# Guard against stray in-repo Chroma stores (e.g. from a mangled store path)
**/chroma.sqlite3
Usersparis*/

# Packaged extension bundles. These are build outputs, are hundreds of MB
# because they carry the whole node_modules tree, and must never be committed.
*.mcpb
